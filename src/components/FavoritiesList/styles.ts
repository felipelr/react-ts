import styled from "styled-components";
import { strab_blue } from "../../utils/colors";

export const FavoriteProfessionals = styled.p`
    font-size: .8em;
    color: ${strab_blue};
    font-weight: normal;
    margin-top: 32px;
    margin-bottom: 8px;
`