import styled from "styled-components";
import { black } from "../../utils/colors";

export const ClientOrderContainer = styled.section`
    padding: 16px;
`
export const ItemTitle = styled.span`
    color: ${black};
    font-size: 1em;
    font-weight: normal;
    margin: 0;
    padding: 0;
`

export const ItemDescription = styled.span`
    color: ${black};
    font-size: .8em;
    font-weight: normal;
    margin: 0;
    padding: 0;
`

export const ItemInfo = styled.span`
    color: ${black};
    font-size: .8em;
    font-weight: normal;
    padding-bottom: 12px;
`

export const InfoContainer = styled.div`
    padding: 8px;
`