import styled from "styled-components";
import { Button } from "@mui/material";

export const ButtonStrab = styled(Button).attrs((props) => ({
  variant: "contained",
  //color: "secondary",
  sx: { mt: 4, mb: 2, width: '100%' },
}))``;