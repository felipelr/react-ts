import styled from "styled-components";
import { black, strab_blue } from "../../utils/colors";

export const ClientHomeContainer = styled.div`
    padding: 16px;
`;

export const Welcome = styled.h3`
    font-size: 1em;
    color: ${black};
    font-weight: normal;
`

export const Title = styled.h1`
    font-size: 2em;
    color: ${strab_blue};
    font-weight: bold;
`

export const FavoriteProfessionals = styled.p`
    font-size: .8em;
    color: ${strab_blue};
    font-weight: normal;
    margin-top: 32px;
    margin-bottom: 8px;
`